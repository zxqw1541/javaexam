작업목표: .properties 파일 읽기, Relfection API를 이용한 인스턴스 자동 생성
.properties 파일?
- key=value이 한줄로 작성된 파일.
- 속성 정보를 저장할 때 사용.
- java.util.Properties 클래스를 이용하여 파일을 읽는다.

리플렉션 API

실습 :
1. 각 기능을 처리하는 클래스를 정보를 menu.properties 파일에 저장한다.
2. ProjectApp에서 menu.properties 파일을 읽는다.
3. 프로퍼티 파일에 등록된 대로 클래스의 인스턴스를 생성하여 맵에 담는다.
(10/19)
4. 변경된 구조에 맞춰 게시판 기능을 추가한다
  - menu.properties 파일에 새 기능을 처리할 클래스 정보를 등록한다.
  - 게시물 데이터 저장할 사용자 정의 데이터 타입을 추가한다. => Board 클래스 추가.
  - 게시물 기능을 처리할 MenuControl 클래스 작성 => v06.BoardControl 클래스를 추가.
  - 게시판 각 메뉴(Command)는 생략!
  - ProjectApp에 게시판 기능 추가. 